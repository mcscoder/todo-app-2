# todo_app_2

```yaml
dependencies:
    flutter_bloc
    go_router
    get_it
    injectable
    equatable
    drift
    drift_flutter
    path_provider
    google_fonts

dev_dependencies:  
    injectable_generator
    build_runner
    drift_dev
```

```bash
flutter pub add flutter_bloc go_router get_it injectable equatable drift drift_flutter path_provider google_fonts dev:injectable_generator dev:build_runner dev:drift_dev
```
